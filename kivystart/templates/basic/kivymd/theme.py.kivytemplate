"""
Theming class for the app.
"""

class Theme:
    # RGBA colors
    # Simple class right?
    primary_color = [0, .1, .5, .8]
    secondary_color = [0, .1, .5, .6]
    background_color = [[ if theme == "dark" ]][0, 0, 0, .8][[ else ]] [1, 1, 1, 1][[ endif ]]
    text_color = [[ if theme == "dark" ]][1, 1, 1, .8][[ else ]][0, 0, 0, .8][[ endif ]]
